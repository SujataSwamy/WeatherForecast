package com.weatherforecast.domain

class Constants {
    companion object {
        val DEFAULT_CONNECT_TIMEOUT_IN_MS: Long = 5000
        val DEFAULT_WRITE_TIMEOUT_IN_MS: Long = 5000
        val DEFAULT_READ_TIMEOUT_IN_MS: Long = 5000
    }
}